import Navbar from '@/Components/Navbar';
import React from 'react';

type XmlProps = {
    
};

const Xml:React.FC<XmlProps> = () => {
    
    return (
        <div>
            <Navbar />
            <h1>XML</h1>
            {/* Other content */}
        </div>
    );
}
export default Xml;